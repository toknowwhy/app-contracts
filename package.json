{
  "name": "hardhat-project",
  "scripts": {
    "clean": "rm -rf cache/ artifacts/",
    "compile": "hardhat compile",
    "deploy:sepolia": "DEPLOY=v1.0.0.sepolia hardhat --network sepolia deploy",
    "deploy:mumbai": "DEPLOY=v1.0.0.mumbai hardhat --network mumbai deploy",
    "generate": "node ./scripts/generateContractList.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers@^0.4.1",
    "chalk": "^4.0.0",
    "ethers": "^6.6.2",
    "hardhat": "^2.16.1",
    "hardhat-deploy": "^0.11.34"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.2"
  }
}
